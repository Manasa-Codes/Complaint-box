<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<style>
     body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f9;
        }
        form {
            background-color: #658b6f;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px #2f575d;
            width: 100%;
            max-width: 450px;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        h1 {
            
            text-align: center;
            color: white; /* Changed to white */
        }
        label {
            font-size: 14px;
            color: white; /* Changed to white */
        }
        input
        {
          
            padding: 8px;
            font-size: 14px;
            border-radius: 4px;
            border: 1px solid #ccc;
            width: 90%; /* Decreased size */
        }
        button {
            width:40%;
            padding: 8px 25px; /* Reduced size */
            background-color: #fff;
            color: #2f575d;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            align-self: center; /* Centered the button */
        }
        button:hover {
            background-color: #2f575d;
            color: white;
        }

        /* Responsive Design */
        @media screen and (max-width: 768px) {
            form {
                width: 90%;
            }
            button {
                width: auto;
            }
        }
</style>
<body>
 
  <form action="/login" method="POST">
    <h1>Login</h1>
    <label for="username">Username:</label>
    <input type="text" id="username" name="username" required><br>

    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required><br>

    <a href="/index"><button type="submit">Login</button></a>
    <p>Don't have an account? <a href="/signup">Sign up here</a></p>
  </form>


</body>
</html>
